version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18

  build:
    commands:
      - echo Build started on `date`
      # You can add commands to build your project here if needed.

artifacts:
  files: '**/*'  # Include all files in the output as individual artifacts

cache:
  paths:
    - '/root/.npm' # Add any other paths to cache as needed
